.TH _PRINTF 3 "November 2025" "Custom Library" "Library Functions Manual"

.SH NAME
_printf \- produce formatted output according to a format string

.SH SYNOPSIS
.B #include "main.h"
.br
.B #include <stdarg.h>
.br
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The _printf() function writes output to the standard output (stdout) according to the
format string pointed to by
 format .

The format string contains ordinary characters, which are written directly
to the output, and conversion specifiers, which begin with the '%'
character and determine how the corresponding arguments are formatted.

.PP
The following conversion specifiers are supported:

.TP
.B %c
Print a single character.
Argument type: int

.TP
.B %s
Print a null-terminated string.
Argument type: char *

.TP
.B %%
Print a literal '%' character.
No argument consumed.

.TP
.B %d, %i
Print a signed integer in decimal notation.
Argument type: int

.PP
Any unknown specifier is printed literally: a '%' followed by the
unrecognized character.

.PP
This implementation does not support flags (such as '-', '+', ' ', '#', or '0'),
field width, precision, or length modifiers.

.SH RETURN VALUE
On success,_printf() returns the total number of characters printed.

If format is NULL or an error occurs, the function returns -1 .

.SH EXAMPLES

.B _printf("Length:[%d, %i]\n", len, len);

-->STDOUT: Length:[39, 39]

.B _printf("Negative:[%d]\n", -762534);

-->STDOUT: Negative:[-762534]


.B _printf("Character:[%c]\n", 'H');

-->STDOUT: Character:[H]

.B _printf("String:[%s]\n", "I am a string !");

-->STDOUT: String:[I am a string !]

.B _printf("Percent:[%%]\n");

-->STDOUT: Percent:[%]


.SH DATE
November 2025

.SH AUTHORS
Written by Auric CHEN, Vincent RENAUD, Aythan CRISTOVAO C#28-Sens.


